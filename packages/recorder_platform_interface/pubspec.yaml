name: recorder_platform_interface
description: "Platform-agnostic recorder contract: capture sources, configuration, capabilities, events and typed errors."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.1
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
