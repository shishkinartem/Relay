name: upload_webdav
description: "WebDAV upload destination for Relay — Koofr, Nextcloud and anything else that speaks the protocol."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.1

dependencies:
  upload_core:
    path: ../upload_core
  http: ^1.2.2
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^6.0.0
