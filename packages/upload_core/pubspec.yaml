name: upload_core
description: "Destination-agnostic upload contract: capabilities, validation, progress events, typed errors and retry policy."
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.13.1

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.0
  flutter_lints: ^6.0.0
